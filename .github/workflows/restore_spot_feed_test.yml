name: restore-spot-feed-test
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run regression test
        run: python3 .github/restore_spot_feed_test.py
