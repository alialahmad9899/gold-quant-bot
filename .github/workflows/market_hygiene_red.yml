name: market-hygiene-red

on:
  push:
    branches: [main]

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tests/test_market_provider_hygiene.py
